# 修正 Cloudflare Pages 對 JS/CSS 的 MIME 類型（避免 application/octet-stream 導致模組載入失敗）
# https://developers.cloudflare.com/pages/configuration/headers/
# 僅在「建置輸出目錄 = dist」時生效；若看到 main.tsx 錯誤，請先設 Build output directory = dist

/assets/*
  Content-Type: application/javascript
  X-Content-Type-Options: nosniff
