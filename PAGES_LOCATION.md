# 頁面檔案位置說明

## 📁 頁面檔案位置

所有頁面元件都位於 `src/pages/` 目錄下：

```
src/pages/
├── Index.tsx      # 首頁 (/)
├── Auth.tsx       # 登入/註冊頁面 (/auth)
├── Admin.tsx      # 管理後台 (/admin)
├── CRM.tsx        # CRM 系統 (/crm)
└── NotFound.tsx   # 404 錯誤頁面 (*)
```

## 🔗 路由配置

路由配置在 `src/App.tsx` 中定義：

### 1. 首頁 (Index)
- **檔案**: `src/pages/Index.tsx`
- **路由**: `/`
- **URL**: https://trinhnai-342f2e80.vercel.app/
- **說明**: 網站首頁，包含服務介紹、預約表單等

### 2. 登入/註冊頁面 (Auth)
- **檔案**: `src/pages/Auth.tsx`
- **路由**: `/auth`
- **URL**: https://trinhnai-342f2e80.vercel.app/auth
- **說明**: 用戶登入和註冊頁面
- **功能**: 
  - 用戶登入
  - 用戶註冊
  - 登入後根據權限導向不同頁面（管理員 → /admin，一般用戶 → /）

### 3. 管理後台 (Admin)
- **檔案**: `src/pages/Admin.tsx`
- **路由**: `/admin`
- **URL**: https://trinhnai-342f2e80.vercel.app/admin
- **權限保護**: 使用 `AdminGuard` 組件保護
- **說明**: 管理後台，用於管理預約和潛在客戶
- **功能**:
  - 查看潛在客戶 (Leads)
  - 查看和管理預約 (Bookings)
  - 篩選和搜尋功能
  - 更新預約狀態
  - 刪除預約

### 4. CRM 系統 (CRM)
- **檔案**: `src/pages/CRM.tsx`
- **路由**: `/crm`
- **URL**: https://trinhnai-342f2e80.vercel.app/crm
- **權限保護**: 使用 `AdminGuard` 組件保護
- **說明**: LINE CRM 系統，用於管理 LINE 用戶
- **功能**:
  - 管理 LINE 用戶
  - 發送訊息
  - 標籤管理
  - 備註管理
  - 再行銷功能

### 5. 404 錯誤頁面 (NotFound)
- **檔案**: `src/pages/NotFound.tsx`
- **路由**: `*` (所有未匹配的路由)
- **說明**: 當訪問不存在的路由時顯示此頁面

## 🔒 權限保護

### AdminGuard 組件
- **檔案**: `src/components/AdminGuard.tsx`
- **功能**: 保護需要管理員權限的頁面
- **保護的頁面**:
  - `/admin` - 管理後台
  - `/crm` - CRM 系統

### 權限檢查流程
1. **未登入**: 自動重定向到 `/auth` 登入頁面
2. **已登入但非管理員**: 顯示「權限不足」頁面，提供返回首頁或切換帳號選項
3. **已登入且為管理員**: 允許訪問頁面

## 📝 如何修改頁面

### 修改登入頁面
編輯檔案：`src/pages/Auth.tsx`

### 修改管理後台
編輯檔案：`src/pages/Admin.tsx`

### 修改 CRM 系統
編輯檔案：`src/pages/CRM.tsx`

### 修改首頁
編輯檔案：`src/pages/Index.tsx`

## 🔍 相關檔案

### 路由配置
- **檔案**: `src/App.tsx`
- **說明**: 包含所有路由定義

### 認證相關
- **Hook**: `src/hooks/useAuth.tsx`
  - 提供用戶認證相關功能
  - 管理用戶狀態、登入、註冊、登出
- **組件**: `src/components/AdminGuard.tsx`
  - 權限保護組件

### Supabase 連線
- **檔案**: `src/integrations/supabase/client.ts`
- **說明**: Supabase 客戶端配置

## 📊 頁面關係圖

```
首頁 (/)
  │
  ├──→ 登入/註冊 (/auth)
  │       │
  │       ├──→ 管理後台 (/admin) [需要管理員權限]
  │       │       │
  │       │       └──→ CRM 系統 (/crm) [需要管理員權限]
  │       │
  │       └──→ 返回首頁 (/)
  │
  └──→ 預約表單 (在首頁)
```

## 🎯 快速參考

### 本地開發
```bash
# 啟動開發伺服器
npm run dev

# 訪問頁面
http://localhost:8080/          # 首頁
http://localhost:8080/auth      # 登入頁
http://localhost:8080/admin     # 管理後台（需登入）
http://localhost:8080/crm       # CRM（需登入）
```

### 生產環境
```
https://trinhnai-342f2e80.vercel.app/          # 首頁
https://trinhnai-342f2e80.vercel.app/auth      # 登入頁
https://trinhnai-342f2e80.vercel.app/admin     # 管理後台（需登入）
https://trinhnai-342f2e80.vercel.app/crm       # CRM（需登入）
```
